# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# uv / virtualenv
.venv/

# Local environment configuration (never commit)
.env

# Docker / compose local overrides
compose.override.yaml

# Local design docs / specs (never commit)
docs/
